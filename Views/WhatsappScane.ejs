<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WhatsApp Scan - Channels / Devices</title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <style>
        /* ===== THEME VARIABLES ===== */
        :root {
            --primary: #3b82f6;
            --primary-dark: #2563eb;
            --primary-light: #60a5fa;
            --success: #10b981;
            --warning: #f59e0b;
            --danger: #ef4444;
            --info: #3b82f6;
            
            /* Light Mode */
            --bg-body: #f3f4f6;
            --bg-white: #ffffff;
            --bg-offwhite: #f8fafc;
            --bg-card: #ffffff;
            --text: #1e293b;
            --text-light: #334155;
            --text-muted: #64748b;
            --dark: #0f172a;
            --border: #eef2f6;
            --shadow: 0 4px 20px rgba(0,0,0,0.02);
            --shadow-lg: 0 10px 25px rgba(0,0,0,0.1);
            
            /* Layout */
            --header-height: 70px;
            --sidebar-width: 280px;
        }

        /* Dark Mode */
        body.dark {
            --bg-body: #0f172a;
            --bg-white: #1e293b;
            --bg-offwhite: #334155;
            --bg-card: #1e293b;
            --text: #f1f5f9;
            --text-light: #cbd5e1;
            --text-muted: #94a3b8;
            --dark: #f8fafc;
            --border: #334155;
            --shadow: 0 4px 20px rgba(0,0,0,0.3);
            --shadow-lg: 0 10px 25px rgba(0,0,0,0.5);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            overflow-x: hidden;
            background: var(--bg-body);
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            color: var(--text);
            transition: background-color 0.3s, color 0.3s;
        }

        /* ===== BORDER MODES ===== */
        body.round-border .header-wrapper,
        body.round-border .sidebar-wrapper,
        body.round-border .mid-content,
        body.round-border .table-responsive-modern,
        body.round-border .search-box input,
        body.round-border .column-toggle-btn,
        body.round-border .column-toggle-menu,
        body.round-border .action-menu-btn,
        body.round-border .action-dropdown,
        body.round-border .status-badge-modern,
        body.round-border .phone-badge,
        body.round-border .ip-address,
        body.round-border .device-icon,
        body.round-border .btn-add-device,
        body.round-border .page-btn,
        body.round-border .modal-content,
        body.round-border .qr-loader,
        body.round-border .toast-notification {
            border-radius: 16px !important;
        }

        body.round-border .device-icon {
            border-radius: 12px !important;
        }

        body.round-border .status-dot {
            border-radius: 50% !important;
        }

        body.round-border .page-btn {
            border-radius: 10px !important;
        }

        body.no-radius .header-wrapper,
        body.no-radius .sidebar-wrapper,
        body.no-radius .mid-content,
        body.no-radius .table-responsive-modern,
        body.no-radius .search-box input,
        body.no-radius .column-toggle-btn,
        body.no-radius .column-toggle-menu,
        body.no-radius .action-menu-btn,
        body.no-radius .action-dropdown,
        body.no-radius .status-badge-modern,
        body.no-radius .phone-badge,
        body.no-radius .ip-address,
        body.no-radius .device-icon,
        body.no-radius .btn-add-device,
        body.no-radius .page-btn,
        body.no-radius .modal-content,
        body.no-radius .qr-loader,
        body.no-radius .toast-notification,
        body.no-radius .device-icon,
        body.no-radius .status-dot {
            border-radius: 0 !important;
        }

        .header-wrapper {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            z-index: 1030;
            background: var(--bg-white);
            box-shadow: var(--shadow);
            border-bottom: 1px solid var(--border);
        }

        .main-wrapper {
            margin-top: 70px;
            display: flex;
            width: 100%;
            min-height: calc(100vh - 70px);
        }

        .sidebar-wrapper {
            width: 280px;
            flex-shrink: 0;
            position: fixed;
            left: 0;
            top: 70px;
            bottom: 0;
            z-index: 1020;
            transition: width 0.3s ease;
            background: var(--bg-white);
            border-right: 1px solid var(--border);
        }

        .sidebar-wrapper.collapsed {
            width: 80px;
        }

        .content-wrapper {
            flex: 1;
            margin-left: 280px;
            padding: 20px;
            transition: margin-left 0.3s ease;
            background: var(--bg-body);
            min-height: calc(100vh - 70px);
        }

        .sidebar-wrapper.collapsed + .content-wrapper {
            margin-left: 80px;
        }

        .mid-content {
            background: var(--bg-card);
            border-radius: 16px;
            padding: 24px;
            box-shadow: var(--shadow);
            border: 1px solid var(--border);
            min-height: 500px;
        }

        @media (max-width: 768px) {
            .sidebar-wrapper {
                transform: translateX(-100%);
                width: 280px !important;
            }
            
            .sidebar-wrapper.mobile-open {
                transform: translateX(0);
            }
            
            .content-wrapper {
                margin-left: 0 !important;
            }
            
            .mid-content {
                padding: 16px;
            }
        }

        /* Table Styles */
        .table-responsive-modern {
            border-radius: 12px;
            border: 1px solid var(--border);
            overflow-x: auto;
            margin-bottom: 20px;
            background: var(--bg-card);
            box-shadow: 0 2px 8px rgba(0,0,0,0.02);
        }

        .table-modern {
            width: 100%;
            border-collapse: collapse;
            min-width: 900px;
        }

        .table-modern th {
            background: var(--bg-offwhite);
            padding: 16px 12px;
            font-size: 13px;
            font-weight: 600;
            color: var(--text-muted);
            text-transform: uppercase;
            letter-spacing: 0.3px;
            border-bottom: 2px solid var(--border);
            white-space: nowrap;
            text-align: left;
        }

        .table-modern td {
            padding: 16px 12px;
            font-size: 14px;
            color: var(--text);
            border-bottom: 1px solid var(--border);
            vertical-align: middle;
            white-space: nowrap;
        }

        .table-modern tbody tr:hover {
            background: var(--bg-offwhite);
        }

        /* Column visibility toggle */
        .column-toggle-dropdown {
            position: relative;
            display: inline-block;
        }

        .column-toggle-btn {
            padding: 10px 20px;
            border-radius: 10px;
            background: var(--bg-white);
            border: 1px solid var(--border);
            color: var(--text);
            font-size: 14px;
            font-weight: 500;
            display: flex;
            align-items: center;
            gap: 8px;
            cursor: pointer;
            transition: all 0.2s;
        }

        .column-toggle-btn:hover {
            background: var(--bg-offwhite);
            border-color: var(--primary);
            color: var(--primary);
        }

        .column-toggle-menu {
            position: absolute;
            top: 100%;
            right: 0;
            min-width: 220px;
            background: var(--bg-white);
            border-radius: 12px;
            box-shadow: var(--shadow-lg);
            z-index: 1000;
            padding: 12px;
            margin-top: 8px;
            border: 1px solid var(--border);
            display: none;
        }

        .column-toggle-menu.show {
            display: block;
            animation: fadeIn 0.2s ease;
        }

        .column-item {
            display: flex;
            align-items: center;
            gap: 10px;
            padding: 8px 10px;
            border-radius: 8px;
            cursor: pointer;
            transition: all 0.2s;
        }

        .column-item:hover {
            background: var(--bg-offwhite);
        }

        .column-item input[type="checkbox"] {
            width: 16px;
            height: 16px;
            cursor: pointer;
            accent-color: var(--primary);
        }

        .column-item label {
            font-size: 13px;
            font-weight: 500;
            color: var(--text);
            cursor: pointer;
            flex: 1;
        }

        /* Pagination Styles */
        .pagination-container {
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 20px;
            margin-top: 20px;
            padding: 16px 0;
            border-top: 1px solid var(--border);
        }

        .pagination-info {
            font-size: 13px;
            color: var(--text-muted);
        }

        .pagination-controls {
            display: flex;
            align-items: center;
            gap: 8px;
            flex-wrap: wrap;
        }

        .rows-per-page {
            display: flex;
            align-items: center;
            gap: 10px;
            margin-right: 15px;
        }

        .rows-per-page select {
            padding: 8px 12px;
            border-radius: 8px;
            border: 1px solid var(--border);
            background: var(--bg-white);
            font-size: 13px;
            color: var(--text);
            font-weight: 500;
            cursor: pointer;
            outline: none;
        }

        .rows-per-page select:focus {
            border-color: var(--primary);
            box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.1);
        }

        .page-btn {
            width: 38px;
            height: 38px;
            border-radius: 10px;
            border: 1px solid var(--border);
            background: var(--bg-white);
            color: var(--text);
            font-size: 14px;
            font-weight: 500;
            cursor: pointer;
            transition: all 0.2s;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .page-btn:hover:not(:disabled) {
            background: var(--primary);
            border-color: var(--primary);
            color: white;
        }

        .page-btn.active {
            background: var(--primary);
            border-color: var(--primary);
            color: white;
        }

        .page-btn:disabled {
            opacity: 0.5;
            cursor: not-allowed;
        }

        .page-numbers {
            display: flex;
            align-items: center;
            gap: 5px;
        }

        .page-numbers .page-btn {
            width: 38px;
        }

        /* Device icon colors */
        .device-name-cell {
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .device-icon {
            width: 36px;
            height: 36px;
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 18px;
            background: var(--bg-offwhite);
        }

        .device-icon.online {
            background: rgba(16, 185, 129, 0.1);
            color: var(--success);
        }

        .device-icon.pending {
            background: rgba(245, 158, 11, 0.1);
            color: var(--warning);
        }

        .device-icon.offline {
            background: rgba(239, 68, 68, 0.1);
            color: var(--danger);
        }

        .device-info {
            display: flex;
            flex-direction: column;
        }

        .device-name {
            font-weight: 600;
            color: var(--dark);
        }

        .device-id {
            font-size: 11px;
            color: var(--text-muted);
            font-family: 'Courier New', monospace;
        }

        .ip-address {
            font-family: 'Courier New', monospace;
            background: var(--bg-offwhite);
            padding: 4px 8px;
            border-radius: 6px;
            font-size: 12px;
            display: inline-block;
            color: var(--text);
        }

        .date-cell {
            display: flex;
            flex-direction: column;
        }

        .date-main {
            font-weight: 500;
            color: var(--dark);
        }

        .date-sub {
            font-size: 11px;
            color: var(--text-muted);
        }

        .status-badge-modern {
            display: inline-flex;
            align-items: center;
            gap: 6px;
            padding: 6px 12px;
            border-radius: 30px;
            font-size: 12px;
            font-weight: 600;
            letter-spacing: 0.3px;
            white-space: nowrap;
        }

        .status-connected {
            background: rgba(16, 185, 129, 0.1);
            color: var(--success);
        }

        .status-pending {
            background: rgba(245, 158, 11, 0.1);
            color: var(--warning);
        }

        .status-offline {
            background: rgba(239, 68, 68, 0.1);
            color: var(--danger);
        }

        .status-dot {
            width: 8px;
            height: 8px;
            border-radius: 50%;
            display: inline-block;
        }

        .status-dot.connected { background: var(--success); }
        .status-dot.pending { background: var(--warning); }
        .status-dot.offline { background: var(--danger); }

        .phone-badge {
            background: var(--bg-offwhite);
            padding: 6px 12px;
            border-radius: 30px;
            font-size: 12px;
            font-weight: 500;
            color: var(--text);
            display: inline-flex;
            align-items: center;
            gap: 6px;
            white-space: nowrap;
        }

        .phone-badge i {
            color: var(--primary);
        }

        .role-badge-mini {
            display: inline-flex;
            align-items: center;
            padding: 2px 8px;
            border-radius: 30px;
            font-size: 10px;
            font-weight: 600;
            margin-right: 6px;
        }
        
        .role-superadmin { background: #8b5cf6; color: white; }
        .role-educator { background: #3b82f6; color: white; }
        .role-trainer { background: #f59e0b; color: white; }
        .role-student { background: #10b981; color: white; }
        .role-user { background: var(--text-muted); color: white; }

        .action-menu {
            position: relative;
            display: inline-block;
        }

        .action-menu-btn {
            width: 36px;
            height: 36px;
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            background: transparent;
            border: 1px solid var(--border);
            color: var(--text-muted);
            cursor: pointer;
            transition: all 0.2s;
        }

        .action-menu-btn:hover {
            background: var(--bg-offwhite);
            color: var(--primary);
            border-color: var(--primary);
        }

        .action-dropdown {
            position: absolute;
            top: 100%;
            right: 0;
            min-width: 220px;
            background: var(--bg-white);
            border-radius: 12px;
            box-shadow: var(--shadow-lg);
            z-index: 1000;
            padding: 8px;
            display: none;
            margin-top: 8px;
            border: 1px solid var(--border);
        }

        .action-dropdown.show {
            display: block;
            animation: fadeIn 0.2s ease;
        }

        .action-item {
            display: flex;
            align-items: center;
            gap: 10px;
            padding: 10px 12px;
            border-radius: 8px;
            cursor: pointer;
            transition: all 0.2s;
            text-decoration: none;
            color: var(--text);
            border: none;
            background: transparent;
            width: 100%;
            text-align: left;
            font-size: 13px;
            font-weight: 500;
        }

        .action-item:hover {
            background: var(--bg-offwhite);
            color: var(--primary);
        }

        .action-item.delete:hover {
            background: rgba(239, 68, 68, 0.1);
            color: var(--danger);
        }

        .menu-divider {
            height: 1px;
            background: var(--border);
            margin: 8px 0;
        }

        .created-by-info {
            padding: 8px 12px;
            background: var(--bg-offwhite);
            border-radius: 8px;
            margin-bottom: 8px;
            font-size: 12px;
            border: 1px solid var(--border);
        }

        .created-by-label {
            color: var(--text-muted);
            font-size: 11px;
            text-transform: uppercase;
            letter-spacing: 0.3px;
            margin-bottom: 6px;
        }

        .created-by-details {
            display: flex;
            align-items: center;
            gap: 8px;
            flex-wrap: wrap;
        }

        .created-by-email {
            color: var(--dark);
            font-weight: 500;
            font-size: 12px;
            max-width: 150px;
            overflow: hidden;
            text-overflow: ellipsis;
            white-space: nowrap;
        }

        .header-section {
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 20px;
            margin-bottom: 24px;
        }

        .header-title h5 {
            font-size: 20px;
            font-weight: 700;
            color: var(--dark);
            margin-bottom: 8px;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .action-bar {
            display: flex;
            gap: 12px;
            flex-wrap: wrap;
        }

        .search-box {
            position: relative;
            min-width: 280px;
        }

        .search-box i {
            position: absolute;
            left: 14px;
            top: 50%;
            transform: translateY(-50%);
            color: var(--text-muted);
            font-size: 14px;
        }

        .search-box input {
            width: 100%;
            padding: 10px 16px 10px 40px;
            border: 1px solid var(--border);
            border-radius: 50px;
            font-size: 14px;
            background: var(--bg-offwhite);
            transition: all 0.3s;
            color: var(--text);
        }

        .search-box input:focus {
            outline: none;
            border-color: var(--primary);
            background: var(--bg-white);
            box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.1);
        }

        .btn-add-device {
            padding: 10px 20px;
            border-radius: 10px;
            background: linear-gradient(135deg, var(--primary), var(--primary-dark));
            color: white;
            border: none;
            font-size: 14px;
            font-weight: 600;
            display: flex;
            align-items: center;
            gap: 8px;
            transition: all 0.3s;
            white-space: nowrap;
        }

        .btn-add-device:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(59, 130, 246, 0.3);
        }

        .empty-state-modern {
            text-align: center;
            padding: 60px 20px;
        }

        .empty-state-modern i {
            font-size: 60px;
            color: var(--text-muted);
            margin-bottom: 20px;
        }

        .empty-state-modern h5 {
            color: var(--text);
            font-size: 18px;
            margin-bottom: 10px;
        }

        .empty-state-modern p {
            color: var(--text-muted);
            max-width: 400px;
            margin: 0 auto 20px;
        }

        .column-hidden {
            display: none;
        }

        #toast {
            position: fixed;
            top: 24px;
            right: 24px;
            width: 320px;
            padding: 14px 16px;
            border-radius: 12px;
            box-shadow: var(--shadow-lg);
            opacity: 0;
            transform: translateY(-15px);
            transition: .3s ease;
            color: #fff;
            z-index: 9999;
        }

        #toast.show {
            opacity: 1;
            transform: translateY(0);
        }

        .toast-row {
            display: flex;
            gap: 12px;
            align-items: flex-start;
        }

        .toast-icon {
            width: 34px;
            height: 34px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 16px;
            background: rgba(0,0,0,.25);
        }

        .toast-text h4 {
            margin: 0;
            font-size: 14px;
            font-weight: 600;
        }

        .toast-text p {
            margin: 4px 0 0;
            font-size: 13px;
            opacity: .9;
        }

        .toast-success { background: var(--success); }
        .toast-error { background: var(--danger); }
        .toast-info { background: var(--info); }
        .toast-warning { background: var(--warning); color: #1f2937; }

        .toast-progress {
            height: 3px;
            border-radius: 4px;
            margin-top: 10px;
            background: rgba(255,255,255,.6);
            animation: toast-load 3s linear forwards;
        }

        @keyframes toast-load {
            from { width: 100%; }
            to { width: 0%; }
        }

        .qr-loader {
            width: 220px;
            height: 220px;
            margin: 0 auto 20px;
            position: relative;
            background: var(--bg-offwhite);
            border-radius: 20px;
            box-shadow: var(--shadow-lg);
            overflow: hidden;
        }

        .qr-loader::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: linear-gradient(
                45deg,
                transparent 30%,
                rgba(37, 211, 102, 0.1) 50%,
                transparent 70%
            );
            animation: shimmer 2s infinite linear;
        }

        .ring {
            position: absolute;
            inset: 0;
            animation: rotate 1.8s linear infinite;
        }

        .dot-big {
            width: 95px;
            height: 95px;
            background: #22c55e;
            border-radius: 50%;
            position: absolute;
            bottom: 20px;
            left: 25px;
            box-shadow: 0 8px 24px rgba(34, 197, 94, 0.4);
            animation: pulse-green 2s infinite ease-in-out;
        }

        .dot-small {
            width: 55px;
            height: 55px;
            background: #1d9bf0;
            border-radius: 50%;
            position: absolute;
            top: 20px;
            right: 25px;
            box-shadow: 0 8px 24px rgba(29, 155, 240, 0.4);
            animation: pulse-blue 2s infinite ease-in-out;
        }

        .whatsapp-center {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            z-index: 2;
            font-size: 40px;
            color: #075e54;
            animation: heartbeat 1.5s infinite ease-in-out;
        }

        .loading-text {
            margin-top: 15px;
            font-size: 14px;
            color: var(--text-muted);
            font-weight: 500;
        }

        .loading-text span {
            animation: dots 1.5s infinite;
            opacity: 0;
        }

        .loading-text span:nth-child(1) { animation-delay: 0.1s; }
        .loading-text span:nth-child(2) { animation-delay: 0.2s; }
        .loading-text span:nth-child(3) { animation-delay: 0.3s; }

        #qrImage {
            width: 220px;
            height: 220px;
            border-radius: 12px;
            box-shadow: var(--shadow-lg);
            border: 2px solid var(--border);
            padding: 10px;
            background: var(--bg-white);
            margin: 0 auto 20px;
        }

        .status-indicator {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            margin-top: 15px;
            font-size: 13px;
        }

        .success-animation {
            animation: slideUp 0.5s ease-out;
        }

        .connected-check {
            color: #22c55e;
            font-size: 24px;
            animation: bounceIn 0.5s ease-out;
        }

        @keyframes rotate { 
            from { transform: rotate(0deg); } 
            to { transform: rotate(360deg); } 
        }
        
        @keyframes pulse-green { 
            0%, 100% { transform: scale(1); } 
            50% { transform: scale(1.05); } 
        }
        
        @keyframes pulse-blue { 
            0%, 100% { transform: scale(1); } 
            50% { transform: scale(1.08); } 
        }
        
        @keyframes shimmer { 
            0% { transform: rotate(0deg); } 
            100% { transform: rotate(360deg); } 
        }
        
        @keyframes heartbeat { 
            0%, 100% { transform: translate(-50%, -50%) scale(1); } 
            50% { transform: translate(-50%, -50%) scale(1.1); } 
        }
        
        @keyframes dots { 
            0%, 100% { opacity: 0; } 
            50% { opacity: 1; } 
        }
        
        @keyframes fadeIn { 
            from { opacity: 0; transform: translateY(-10px); } 
            to { opacity: 1; transform: translateY(0); } 
        }
        
        @keyframes slideUp { 
            from { opacity: 0; transform: translateY(10px); } 
            to { opacity: 1; transform: translateY(0); } 
        }
        
        @keyframes bounceIn { 
            0% { transform: scale(0); } 
            70% { transform: scale(1.2); } 
            100% { transform: scale(1); } 
        }

        @media (max-width: 768px) {
            .header-section {
                flex-direction: column;
                align-items: flex-start;
            }
            
            .action-bar {
                width: 100%;
                flex-direction: column;
            }
            
            .search-box {
                width: 100%;
            }
            
            .btn-add-device {
                width: 100%;
                justify-content: center;
            }
            
            .table-modern {
                min-width: 800px;
            }
            
            .pagination-container {
                flex-direction: column;
                align-items: flex-start;
            }
            
            .pagination-controls {
                width: 100%;
                justify-content: space-between;
            }
        }
    </style>
</head>
<body>
    <!-- Theme Initialization Script -->
    <script>
        (function() {
            const savedTheme = localStorage.getItem('theme') || 'light';
            const savedBorder = localStorage.getItem('border') || 'round-border';
            document.body.classList.add(savedTheme);
            document.body.classList.add(savedBorder);
            console.log('🎨 Theme loaded:', savedTheme);
            console.log('📐 Border mode loaded:', savedBorder);
        })();
    </script>

    <!-- HEADER -->
    <div class="header-wrapper">
        <%- include('../partials/ram/header') %>
    </div>

    <!-- MAIN LAYOUT -->
    <div class="main-wrapper">
        <!-- SIDEBAR -->
        <div class="sidebar-wrapper" id="sidebarWrapper">
            <%- include('../partials/ram/sidebaar') %>
        </div>

        <!-- CONTENT -->
        <div class="content-wrapper" id="contentWrapper">
            <div class="mid-content">
                <!-- Toast Container -->
                <div id="toast"></div>

                <!-- HEADER SECTION -->
                <div class="header-section">
                    <div class="header-title">
                        <h5>
                            <i class="fab fa-whatsapp text-success"></i> 
                            WhatsApp Devices
                        </h5>
                    </div>

                    <div class="action-bar">
                        <div class="search-box">
                            <i class="fas fa-search"></i>
                            <input type="text" placeholder="Search devices..." id="searchInput">
                        </div>
                        
                        <!-- Column Visibility Toggle -->
                        <div class="column-toggle-dropdown">
                            <button class="column-toggle-btn" onclick="toggleColumnMenu(event)">
                                <i class="fas fa-table"></i> Columns
                                <i class="fas fa-chevron-down"></i>
                            </button>
                            <div class="column-toggle-menu" id="columnMenu">
                                <div class="column-item">
                                    <input type="checkbox" id="colDevice" checked onchange="toggleColumn(0, this.checked)">
                                    <label for="colDevice">Device</label>
                                </div>
                                <div class="column-item">
                                    <input type="checkbox" id="colWhatsApp" checked onchange="toggleColumn(1, this.checked)">
                                    <label for="colWhatsApp">WhatsApp Name</label>
                                </div>
                                <div class="column-item">
                                    <input type="checkbox" id="colIP" checked onchange="toggleColumn(2, this.checked)">
                                    <label for="colIP">IP Address</label>
                                </div>
                                <div class="column-item">
                                    <input type="checkbox" id="colAdded" checked onchange="toggleColumn(3, this.checked)">
                                    <label for="colAdded">Added On</label>
                                </div>
                                <div class="column-item">
                                    <input type="checkbox" id="colStatus" checked onchange="toggleColumn(4, this.checked)">
                                    <label for="colStatus">Status</label>
                                </div>
                                <div class="column-item">
                                    <input type="checkbox" id="colNumber" checked onchange="toggleColumn(5, this.checked)">
                                    <label for="colNumber">WhatsApp Number</label>
                                </div>
                                <div class="column-item">
                                    <input type="checkbox" id="colActions" checked onchange="toggleColumn(6, this.checked)">
                                    <label for="colActions">Actions</label>
                                </div>
                            </div>
                        </div>

                        <button class="btn-add-device" onclick="openQrModal()">
                            <i class="fas fa-plus-circle"></i> Add New Device
                        </button>
                    </div>
                </div>

                <!-- TABLE -->
                <div class="table-responsive-modern">
                    <table class="table-modern" id="deviceTable">
                        <thead>
                            <tr id="tableHeader">
                                <th>Device</th>
                                <th>WhatsApp Name</th>
                                <th>IP Address</th>
                                <th>Added On</th>
                                <th>Status</th>
                                <th>WhatsApp Number</th>
                                <th class="text-center">Actions</th>
                            </tr>
                        </thead>
                        <tbody id="deviceTableBody">
                            <% if (devices && devices.length > 0) { %>
                                <% devices.forEach(device => { 
                                    const statusClass = device.connectionStatus === 'online' ? 'connected' : 
                                                       device.connectionStatus === 'pending' ? 'pending' : 'offline';
                                %>
                                <tr class="device-row" data-device-id="<%= device.deviceId %>">
                                    <td>
                                        <div class="device-name-cell">
                                            <div class="device-icon <%= statusClass %>">
                                                <i class="fab fa-whatsapp"></i>
                                            </div>
                                            <div class="device-info">
                                                <span class="device-name">WhatsApp Device</span>
                                                <span class="device-id"><%= device.deviceId.substring(0, 10) %>...</span>
                                            </div>
                                        </div>
                                    </td>
                                    <td>
                                        <div class="whatsapp-name">
                                            <strong><%= device.whatsappName || 'Not Available' %></strong>
                                        </div>
                                    </td>
                                    <td>
                                        <span class="ip-address"><%= device.ipAddress || '-' %></span>
                                    </td>
                                    <td>
                                        <div class="date-cell">
                                            <% if (device.deviceAddedAt) { %>
                                                <span class="date-main"><%= new Date(device.deviceAddedAt).toLocaleDateString() %></span>
                                                <span class="date-sub"><%= new Date(device.deviceAddedAt).toLocaleTimeString() %></span>
                                            <% } else if (device.createdAt) { %>
                                                <span class="date-main"><%= new Date(device.createdAt).toLocaleDateString() %></span>
                                                <span class="date-sub"><%= new Date(device.createdAt).toLocaleTimeString() %></span>
                                            <% } else { %>
                                                <span class="text-muted">-</span>
                                            <% } %>
                                        </div>
                                    </td>
                                    <td>
                                        <span class="status-badge-modern status-<%= statusClass %>">
                                            <span class="status-dot <%= statusClass %>"></span>
                                            <%= device.connectionStatus?.toUpperCase() || 'OFFLINE' %>
                                        </span>
                                    </td>
                                    <td>
                                        <% if (device.whatsappNumber) { %>
                                            <span class="phone-badge">
                                                <i class="fas fa-phone-alt"></i> <%= device.whatsappNumber %>
                                            </span>
                                        <% } else if (device.phone) { %>
                                            <span class="phone-badge">
                                                <i class="fas fa-phone-alt"></i> <%= device.phone %>
                                            </span>
                                        <% } else { %>
                                            <span class="text-muted">—</span>
                                        <% } %>
                                    </td>
                                    
                                    <td class="text-center">
                                        <div class="action-menu">
                                            <button class="action-menu-btn" onclick="toggleActionMenu(event, '<%= device.deviceId %>')">
                                                <i class="fas fa-ellipsis-v"></i>
                                            </button>
                                            <div class="action-dropdown" id="actionMenu-<%= device.deviceId %>">
                                                
                                                <% if (user.role === 'SuperAdmin') { %>
                                                <div class="created-by-info">
                                                    <div class="created-by-label">
                                                        <i class="fas fa-user-circle me-1"></i> Created By
                                                    </div>
                                                    <div class="created-by-details">
                                                        <span class="role-badge-mini role-<%= device.createdByRole?.toLowerCase() || 'user' %>">
                                                            <%= device.createdByRole || 'User' %>
                                                        </span>
                                                        <span class="created-by-email" title="<%= device.createdByEmail || '' %>">
                                                            <%= device.createdByEmail || 'Unknown' %>
                                                        </span>
                                                    </div>
                                                </div>
                                                <div class="menu-divider"></div>
                                                <% } %>
                                                
                                                <button class="action-item" onclick="copyToken('<%= device.token %>')">
                                                    <i class="fas fa-copy"></i> Copy Token
                                                </button>
                                                <button class="action-item delete" onclick="deleteDevice('<%= device.deviceId %>')">
                                                    <i class="fas fa-trash-alt"></i> Delete Device
                                                </button>
                                            </div>
                                        </div>
                                    </td>
                                </tr>
                                <% }) %>
                            <% } else { %>
                                <tr id="noDevicesRow">
                                    <td colspan="7">
                                        <div class="empty-state-modern">
                                            <i class="fas fa-wifi-slash"></i>
                                            <h5>No WhatsApp Devices Found</h5>
                                            <p>
                                                <% if (user.role === 'SuperAdmin') { %>
                                                    No devices have been added by any user yet.
                                                <% } else { %>
                                                    You haven't added any WhatsApp devices yet.
                                                <% } %>
                                            </p>
                                        </div>
                                    </td>
                                </tr>
                            <% } %>
                        </tbody>
                    </table>
                </div>

                <!-- Pagination Section -->
                <div class="pagination-container" id="paginationContainer" style="<% if (!devices || devices.length === 0) { %>display: none;<% } %>">
                    <div class="pagination-info" id="paginationInfo">
                        Showing 0 to 0 of 0 entries
                    </div>
                    <div class="pagination-controls">
                        <div class="rows-per-page">
                            <span>Show:</span>
                            <select id="rowsPerPage" onchange="changeRowsPerPage()">
                                <option value="5">5</option>
                                <option value="10" selected>10</option>
                                <option value="15">15</option>
                                <option value="20">20</option>
                                <option value="30">30</option>
                                <option value="50">50</option>
                                <option value="100">100</option>
                            </select>
                        </div>
                        <button class="page-btn" id="prevPage" onclick="changePage('prev')" disabled>
                            <i class="fas fa-chevron-left"></i>
                        </button>
                        <div class="page-numbers" id="pageNumbers"></div>
                        <button class="page-btn" id="nextPage" onclick="changePage('next')" disabled>
                            <i class="fas fa-chevron-right"></i>
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- QR MODAL -->
    <div class="modal fade" id="qrModal" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">
                        <i class="fab fa-whatsapp text-success me-2"></i>
                        Scan QR Code
                    </h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>

                <div class="modal-body text-center py-4">
                    <!-- Loader -->
                    <div id="qrLoader" class="qr-loader">
                        <div class="ring">
                            <div class="dot-big"></div>
                            <div class="dot-small"></div>
                        </div>
                        <div class="whatsapp-center">
                            <i class="fab fa-whatsapp"></i>
                        </div>
                    </div>

                    <!-- Loading Text -->
                    <div id="loadingText" class="loading-text">
                        Preparing QR<span>.</span><span>.</span><span>.</span>
                    </div>

                    <!-- QR Code Container -->
                    <div id="qrContainer" style="display:none">
                        <img id="qrImage" width="220"/>
                        
                        <div class="status-indicator">
                            <div class="spinner-border spinner-border-sm text-primary" role="status">
                                <span class="visually-hidden">Loading...</span>
                            </div>
                            <span class="text-muted">Waiting for scan...</span>
                        </div>
                        
                        <p class="mt-3 text-muted" style="font-size:13px">
                            <i class="fas fa-info-circle me-1"></i>
                            Open WhatsApp → Menu → Linked Devices → Scan QR
                        </p>
                    </div>

                    <!-- Success Message -->
                    <div id="successMessage" class="success-animation" style="display:none">
                        <div class="connected-check">
                            <i class="fas fa-check-circle"></i>
                        </div>
                        <h6 class="mt-3 text-success fw-bold">Device Connected Successfully!</h6>
                        <p class="text-muted small">
                            Refreshing page...
                        </p>
                    </div>
                </div>

                <div class="modal-footer">
                    <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">
                        <i class="fas fa-times me-1"></i> Cancel
                    </button>
                    <button type="button" class="btn btn-primary" onclick="refreshQR()" id="refreshBtn" style="display:none">
                        <i class="fas fa-redo-alt me-1"></i> Refresh QR
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

    <script>
        // GLOBAL VARIABLES
        let currentDeviceId;
        let pollInterval;
        let allDevices = [];
        let filteredDevices = [];
        let currentPage = 1;
        let rowsPerPage = 10;

        // ======================
        // INITIALIZATION
        // ======================
        document.addEventListener('DOMContentLoaded', function() {
            // Sidebar sync
            const sidebar = document.getElementById('mainSidebar');
            const sidebarWrapper = document.getElementById('sidebarWrapper');
            
            if (sidebar && sidebarWrapper) {
                if (sidebar.classList.contains('collapsed')) {
                    sidebarWrapper.classList.add('collapsed');
                }
                
                window.toggleCollapse = function() {
                    if (sidebar.classList.contains('collapsed')) {
                        sidebar.classList.remove('collapsed');
                        sidebarWrapper.classList.remove('collapsed');
                    } else {
                        sidebar.classList.add('collapsed');
                        sidebarWrapper.classList.add('collapsed');
                    }
                };
                
                window.toggleMobileSidebar = function() {
                    if (sidebar.classList.contains('open')) {
                        sidebar.classList.remove('open');
                        sidebarWrapper.classList.remove('mobile-open');
                    } else {
                        sidebar.classList.add('open');
                        sidebarWrapper.classList.add('mobile-open');
                    }
                };
                
                window.closeMobileSidebar = function() {
                    sidebar.classList.remove('open');
                    sidebarWrapper.classList.remove('mobile-open');
                };
            }

            // Load all devices from table
            loadDevicesFromTable();
            
            // Initialize pagination
            initializePagination();
            
            // Show welcome toast
            showToast('WhatsApp Devices', 'Welcome <%= user.email %> (<%= user.role %>)', 'info');
            
            // Close column menu when clicking outside
            document.addEventListener('click', function(event) {
                if (!event.target.closest('.column-toggle-dropdown')) {
                    document.getElementById('columnMenu').classList.remove('show');
                }
            });
        });

        // ======================
        // COLUMN VISIBILITY TOGGLE
        // ======================
        function toggleColumnMenu(event) {
            event.stopPropagation();
            document.getElementById('columnMenu').classList.toggle('show');
        }

        function toggleColumn(index, isVisible) {
            const headers = document.querySelectorAll('#tableHeader th');
            const rows = document.querySelectorAll('.device-row');
            
            // Toggle header
            if (headers[index]) {
                if (isVisible) {
                    headers[index].classList.remove('column-hidden');
                } else {
                    headers[index].classList.add('column-hidden');
                }
            }
            
            // Toggle all cells in this column
            rows.forEach(row => {
                const cells = row.querySelectorAll('td');
                if (cells[index]) {
                    if (isVisible) {
                        cells[index].classList.remove('column-hidden');
                    } else {
                        cells[index].classList.add('column-hidden');
                    }
                }
            });
            
            // Save preference to localStorage
            saveColumnPreferences();
        }

        function saveColumnPreferences() {
            const preferences = [];
            for (let i = 0; i < 7; i++) {
                const checkbox = document.getElementById(`col${getColumnName(i)}`);
                preferences.push(checkbox.checked);
            }
            localStorage.setItem('columnPreferences', JSON.stringify(preferences));
        }

        function loadColumnPreferences() {
            const saved = localStorage.getItem('columnPreferences');
            if (saved) {
                const preferences = JSON.parse(saved);
                preferences.forEach((checked, index) => {
                    const checkbox = document.getElementById(`col${getColumnName(index)}`);
                    if (checkbox) {
                        checkbox.checked = checked;
                        toggleColumn(index, checked);
                    }
                });
            }
        }

        function getColumnName(index) {
            const names = ['Device', 'WhatsApp', 'IP', 'Added', 'Status', 'Number', 'Actions'];
            return names[index];
        }

        // ======================
        // PAGINATION FUNCTIONS
        // ======================
        function loadDevicesFromTable() {
            const rows = document.querySelectorAll('.device-row');
            allDevices = Array.from(rows).map(row => ({
                element: row,
                deviceId: row.dataset.deviceId,
                searchText: row.textContent.toLowerCase()
            }));
            filteredDevices = [...allDevices];
        }

        function initializePagination() {
            if (allDevices.length === 0) {
                document.getElementById('paginationContainer').style.display = 'none';
                return;
            }
            document.getElementById('paginationContainer').style.display = 'flex';
            updatePagination();
        }

        function changeRowsPerPage() {
            rowsPerPage = parseInt(document.getElementById('rowsPerPage').value);
            currentPage = 1;
            updatePagination();
        }

        function changePage(direction) {
            const totalPages = Math.ceil(filteredDevices.length / rowsPerPage);
            if (direction === 'prev' && currentPage > 1) {
                currentPage--;
            } else if (direction === 'next' && currentPage < totalPages) {
                currentPage++;
            }
            updatePagination();
        }

        function goToPage(page) {
            currentPage = page;
            updatePagination();
        }

        function updatePagination() {
            const totalItems = filteredDevices.length;
            const totalPages = Math.ceil(totalItems / rowsPerPage);
            
            // Adjust current page if out of bounds
            if (currentPage < 1) currentPage = 1;
            if (currentPage > totalPages) currentPage = totalPages || 1;
            
            // Calculate start and end indices
            const start = (currentPage - 1) * rowsPerPage;
            const end = Math.min(start + rowsPerPage, totalItems);
            
            // Hide all devices first
            allDevices.forEach(device => {
                device.element.style.display = 'none';
            });
            
            // Show only devices for current page
            for (let i = start; i < end; i++) {
                if (filteredDevices[i]) {
                    filteredDevices[i].element.style.display = '';
                }
            }
            
            // Update pagination info
            document.getElementById('paginationInfo').textContent = 
                `Showing ${totalItems > 0 ? start + 1 : 0} to ${end} of ${totalItems} entries`;
            
            // Update page numbers
            updatePageNumbers(totalPages);
            
            // Update prev/next buttons
            document.getElementById('prevPage').disabled = currentPage === 1 || totalItems === 0;
            document.getElementById('nextPage').disabled = currentPage === totalPages || totalItems === 0;
        }

        function updatePageNumbers(totalPages) {
            const pageNumbersContainer = document.getElementById('pageNumbers');
            pageNumbersContainer.innerHTML = '';
            
            if (totalPages <= 7) {
                // Show all pages
                for (let i = 1; i <= totalPages; i++) {
                    addPageButton(i);
                }
            } else {
                // Show first page
                addPageButton(1);
                
                if (currentPage > 3) {
                    addEllipsis();
                }
                
                // Show pages around current page
                for (let i = Math.max(2, currentPage - 1); i <= Math.min(totalPages - 1, currentPage + 1); i++) {
                    if (i > 1 && i < totalPages) {
                        addPageButton(i);
                    }
                }
                
                if (currentPage < totalPages - 2) {
                    addEllipsis();
                }
                
                // Show last page
                if (totalPages > 1) {
                    addPageButton(totalPages);
                }
            }
        }

        function addPageButton(pageNum) {
            const button = document.createElement('button');
            button.className = `page-btn ${pageNum === currentPage ? 'active' : ''}`;
            button.textContent = pageNum;
            button.onclick = () => goToPage(pageNum);
            document.getElementById('pageNumbers').appendChild(button);
        }

        function addEllipsis() {
            const span = document.createElement('span');
            span.className = 'page-btn';
            span.textContent = '...';
            span.disabled = true;
            document.getElementById('pageNumbers').appendChild(span);
        }

        // ======================
        // SEARCH FUNCTIONALITY
        // ======================
        document.getElementById('searchInput').addEventListener('input', function() {
            const searchTerm = this.value.toLowerCase().trim();
            
            if (searchTerm === '') {
                filteredDevices = [...allDevices];
            } else {
                filteredDevices = allDevices.filter(device => 
                    device.searchText.includes(searchTerm)
                );
            }
            
            currentPage = 1;
            updatePagination();
            
            // Show empty state if no results
            const noResultsRow = document.getElementById('noResultsRow');
            if (filteredDevices.length === 0 && allDevices.length > 0) {
                if (!noResultsRow) {
                    const tbody = document.getElementById('deviceTableBody');
                    const row = document.createElement('tr');
                    row.id = 'noResultsRow';
                    row.innerHTML = `
                        <td colspan="7">
                            <div class="empty-state-modern">
                                <i class="fas fa-search"></i>
                                <h5>No Results Found</h5>
                                <p>No devices match your search criteria.</p>
                            </div>
                        </td>
                    `;
                    tbody.appendChild(row);
                }
            } else if (noResultsRow) {
                noResultsRow.remove();
            }
        });

        // ======================
        // ACTION MENU FUNCTIONS
        // ======================
        function toggleActionMenu(event, deviceId) {
            event.stopPropagation();
            
            // Close all other open menus
            document.querySelectorAll('.action-dropdown.show').forEach(menu => {
                if (menu.id !== `actionMenu-${deviceId}`) {
                    menu.classList.remove('show');
                }
            });
            
            // Toggle current menu
            const menu = document.getElementById(`actionMenu-${deviceId}`);
            if (menu) {
                menu.classList.toggle('show');
            }
        }

        // Close action menus when clicking outside
        document.addEventListener('click', function(event) {
            if (!event.target.closest('.action-menu')) {
                document.querySelectorAll('.action-dropdown.show').forEach(menu => {
                    menu.classList.remove('show');
                });
            }
        });

        // ======================
        // TOAST FUNCTION
        // ======================
        function showToast(title, message, type = 'info') {
            const toast = document.getElementById('toast');
            
            if (window.toastTimeout) {
                clearTimeout(window.toastTimeout);
            }
            
            const icons = {
                'success': '✅',
                'error': '❌',
                'warning': '⚠️',
                'info': 'ℹ️'
            };
            
            const icon = icons[type] || 'ℹ️';
            
            toast.className = '';
            toast.innerHTML = `
                <div class="toast-row">
                    <div class="toast-icon">${icon}</div>
                    <div class="toast-text">
                        <h4>${title}</h4>
                        <p>${message}</p>
                    </div>
                </div>
                <div class="toast-progress"></div>
            `;
            
            toast.classList.add(`toast-${type}`, 'show');
            
            window.toastTimeout = setTimeout(() => {
                toast.classList.remove('show');
            }, 3000);
        }

        // ======================
        // QR MODAL FUNCTIONS
        // ======================
        function openQrModal(){
            // Reset UI
            document.getElementById('qrLoader').style.display = 'block';
            document.getElementById('loadingText').style.display = 'block';
            document.getElementById('qrContainer').style.display = 'none';
            document.getElementById('successMessage').style.display = 'none';
            document.getElementById('refreshBtn').style.display = 'none';
            
            // Show modal
            const modal = new bootstrap.Modal(document.getElementById('qrModal'));
            modal.show();
            
            showToast('Creating Device', 'Generating new WhatsApp device...', 'info');
            
            fetch('/add-device', { method:'POST' })
            .then(r => r.json())
            .then(data => {
                if (data.status) {
                    currentDeviceId = data.deviceId;
                    showToast('Device Created', 'QR code generating...', 'success');
                    startPolling();
                } else {
                    showToast('Error', data.msg || 'Failed to create device', 'error');
                }
            })
            .catch(error => {
                console.error('Error:', error);
                showToast('Error', 'Failed to generate QR. Please try again.', 'error');
            });
        }

        function startPolling(){
            clearInterval(pollInterval);
            
            pollInterval = setInterval(() => {
                fetch('/get-qr/' + currentDeviceId)
                .then(r => r.json())
                .then(data => {
                    if (data.status === 'CONNECTED') {
                        clearInterval(pollInterval);
                        
                        document.getElementById('qrLoader').style.display = 'none';
                        document.getElementById('loadingText').style.display = 'none';
                        document.getElementById('qrContainer').style.display = 'none';
                        document.getElementById('successMessage').style.display = 'block';
                        
                        showToast('Success', 'Device connected successfully!', 'success');
                        
                        setTimeout(() => {
                            location.reload();
                        }, 2000);
                    }
                    else if (data.qr) {
                        document.getElementById('qrLoader').style.display = 'none';
                        document.getElementById('loadingText').style.display = 'none';
                        document.getElementById('qrContainer').style.display = 'block';
                        document.getElementById('qrImage').src = data.qr;
                        document.getElementById('refreshBtn').style.display = 'block';
                        
                        showToast('QR Ready', 'Scan the QR code with WhatsApp', 'info');
                    }
                })
                .catch(error => {
                    console.error('Error polling QR:', error);
                });
            }, 1500);
        }

        function refreshQR() {
            if (currentDeviceId) {
                document.getElementById('qrContainer').style.display = 'none';
                document.getElementById('qrLoader').style.display = 'block';
                document.getElementById('loadingText').style.display = 'block';
                
                showToast('Refreshing', 'Generating new QR code...', 'info');
                
                startPolling();
            }
        }

        // ======================
        // UTILITY FUNCTIONS
        // ======================
        function copyToken(token){
            document.querySelectorAll('.action-dropdown.show').forEach(menu => {
                menu.classList.remove('show');
            });
            
            navigator.clipboard.writeText(token);
            showToast('Copied!', 'Token copied to clipboard', 'success');
        }

        function deleteDevice(deviceId){
            document.querySelectorAll('.action-dropdown.show').forEach(menu => {
                menu.classList.remove('show');
            });
            
            if (!confirm('Are you sure you want to delete this device?')) {
                return;
            }
            
            showToast('Deleting Device', 'Removing device from system...', 'info');
            
            fetch('/delete-device/' + deviceId, { method:'DELETE' })
            .then(r => r.json())
            .then(data => {
                if (data.status) {
                    showToast('Deleted', 'Device deleted successfully!', 'success');
                    setTimeout(() => location.reload(), 1000);
                } else {
                    showToast('Error', data.msg || 'Failed to delete device', 'error');
                }
            })
            .catch(error => {
                console.error('Error deleting device:', error);
                showToast('Error', 'Error deleting device. Please try again.', 'error');
            });
        }

        // Clean up intervals when modal is closed
        document.getElementById('qrModal').addEventListener('hidden.bs.modal', function() {
            clearInterval(pollInterval);
            showToast('Cancelled', 'QR code generation cancelled', 'warning');
        });
    </script>
</body>
</html>